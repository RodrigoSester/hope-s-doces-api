const personService = require('./_person.service');
const userService = require('./_user.service');
const orderService = require('./_order.service');

module.exports = {
  personService,
  userService,
  orderService
};
